# Core
python-dotenv>=1.0.0
certifi>=2024.0.0          # CA bundle for HTTPS (urllib on macOS python.org)

# Database
psycopg[binary]>=3.1.0      # psycopg3 — used by API and notebooks
SQLAlchemy>=2.0.0
ipython-sql>=0.5.0          # %%sql magic in notebooks
prettytable>=2.5,<3         # v3 breaks ipython-sql table display (KeyError: 'DEFAULT')

# ML
scikit-learn>=1.5.0
numpy>=2.1.0
pandas>=2.2.0
pyarrow>=14.0.0             # Parquet export training (TRAINING_PARQUET / BigQuery extract)
scipy>=1.14.0
joblib>=1.4.0
imbalanced-learn>=0.13.0    # SMOTE + pipelines (optional in train.py)

# Visualization / bias audit
matplotlib>=3.9.0
seaborn>=0.13.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# AI
anthropic>=0.40.0

# MongoDB
pymongo>=4.8.0

# Notebooks
jupyter>=1.0.0
ipykernel>=6.0.0
